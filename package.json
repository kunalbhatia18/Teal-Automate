{
  "name": "automatemeetings-landing",
  "version": "1.0.0",
  "description": "AutomateMeetings landing page",
  "scripts": {
    "build": "node scripts/build/build.js",
    "serve": "python3 server.py",
    "dev": "python3 -m http.server 3000 --bind 0.0.0.0",
    "clean": "rm -rf dist",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "keywords": ["landing-page", "automatemeetings"],
  "author": "AutomateMeetings",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}